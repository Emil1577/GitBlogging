

 {{#if loggedIn}}
   {{#each blogs as |blog|}}

 
  <section class="flex-row justify-center">
  <h3 class="gallery-name">{{blog.name}}</h3>
  <h3 class="gallery-name">{{blog.title}}</h3>
  <h3 class="gallery-name">{{blog.contents}}</h3>
  <h4>Created: {{format_time blog.created_date}}</h4>



    {{/each}}
  </section>
</section>
